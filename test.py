import numpy as np



# x = np.array([[1,2], [30,4]])

# max_index = np.argmax(x, axis=1)

# r = 5

# max_indices = list(range(len(x))), max_index

# x[max_indices] += r

# print("x", x)
# print("max_index", max_index)

step = 0.01
low = 0
up = 1
range_step = np.arange(low, up + step, step)
print(range_step)
